class Solution {
    public int solution(int n, int k) {
        int answer = 0;
      
        int lambSkewerCost = n * 12000;
        
        int drinkCost = Math.max(k - (n / 10), 0) * 2000;
     
        answer = lambSkewerCost + drinkCost;
        
        return answer;
    }
}
